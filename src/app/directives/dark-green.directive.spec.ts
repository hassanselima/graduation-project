import { DarkGreenDirective } from '../dark-green.directive';

describe('DarkGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkGreenDirective();
    expect(directive).toBeTruthy();
  });
});
